#include<stdio.h>
/**
 * main - prints all possible different combinations of two digits
 * Return: ALways 0 (Success)
 */
int main(void)
{
int i;
for (i = 0; i < 9 ; i++)
{
for ( m = 1 ; m < 10 ; m++)
{
if (m > n)
{
putchar(m);
putchar(n);
}
}
}
putchar('\n');
return (0);
}
