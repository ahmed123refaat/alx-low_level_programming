#include"main.h"
/**
* _puts - prints a string, followed by a new line, to stdout
* @str: string to print
*/
void _puts(char *str)
{
char i;
int n;
for (i = 'a'; i <= n ; i++)
{
_putchar("%c\n", i);
}
*str = n;
_putchar("%c\n", *str);
}
